# @package _global_

# defaults
defaults:
  - override /paths: default
  - override /model: t5_base
  - override /data: t5_pretraining
  - override /trainer: default
  - override /logger: wandb
  - override /callbacks: default

experiment_name: t5_base_scientific_pretraining

seed: 42

# training settings
trainer:
  max_epochs: 5

test: false
train: true 