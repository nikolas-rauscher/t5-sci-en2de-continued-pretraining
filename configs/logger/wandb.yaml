# https://wandb.ai

# Lightning Logger (für train.py etc.)
wandb:
  _target_: lightning.pytorch.loggers.wandb.WandbLogger
  # name: "" # name of the run (normally generated by wandb)
  save_dir: "${paths.output_dir}"
  offline: False
  id: null # pass correct id to resume experiment!
  anonymous: null # enable anonymous logging
  api_key: ${oc.env:WANDB_API_KEY} # API-Key aus Umgebungsvariable
  project: "BA"  # dein projekt name
  log_model: False # upload lightning ckpts
  prefix: "" # a string to put at the beginning of metric keys
  # entity: "" # set to name of your wandb team
  group: ""
  tags: []
  job_type: ""

# Basis-Config für alle DataTrove Pipelines
datatrove:
  project: "BA-DataTrove"
  group: "datatrove-pipelines"
  tags: ["datatrove"]
  job_type: "data-processing"
  save_code: true
  notes: ""